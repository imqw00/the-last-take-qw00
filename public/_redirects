# Serve the actual HTML pages normally
# (No catch-all redirect to index.html)

# Optional: redirect root to index.html explicitly (usually default)
# /    /index.html   200

# If you have any special API or function routes, add them here
# Example:
# /api/*    /.netlify/functions/:splat    200

